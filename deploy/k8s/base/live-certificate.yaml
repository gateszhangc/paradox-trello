apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: paradox-trello-lol-tls
spec:
  secretName: paradox-trello-lol-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - paradox-trello.lol
    - www.paradox-trello.lol
